package utils

import "github.com/d-rov/digital-dopamine/api/models"

// TODO: need to modify the data to only send up what i want to display
// [{site: string, category: string}, ...]

func ParseCategories(categories []models.Classification) {
	// take categories
	// create a list of objects
	// one object per site
	// each site has a category
}
